export const usePartnerStack = () => {
  const reportSignup = (_email: string, _firstName: string) => {
    // Cloud partner tracking removed — Qadam Flow is fully self-hosted
  };

  return { reportSignup };
};
