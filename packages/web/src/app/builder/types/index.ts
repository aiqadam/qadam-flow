export enum RightSideBarType {
  NONE = 'none',
  QADAM_SETTINGS = 'qadam-settings',
  VERSIONS = 'versions',
  RUNS = 'runs',
}

export enum ChatDrawerSource {
  TEST_FLOW = 'test-flow',
  TEST_STEP = 'test-step',
}
