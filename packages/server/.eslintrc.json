{
  "extends": ["../../.eslintrc.json"],
  "ignorePatterns": ["!**/*"],
  "overrides": [
    {
      "files": ["*.ts", "*.tsx"],
      "plugins": ["unused-imports"],
      "rules": {
        "unused-imports/no-unused-imports": "error",
        "@typescript-eslint/no-unused-vars": [
          "error",
          {
            "varsIgnorePattern": "^_",
            "argsIgnorePattern": "^_"
          }
        ]
      }
    },
    {
      "files": ["*.ts", "*.tsx", "*.js", "*.jsx"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "axios",
                "importNames": ["default"],
                "message": "Raw axios default import bypasses request-filtering-agent's SSRF protection (private/loopback/link-local/cloud-metadata IPs). Use safeHttp.axios / safeHttp.createAxios({ ... }) from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
              }
            ]
          }
        ],
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.name='fetch']",
            "message": "Raw fetch(...) bypasses request-filtering-agent's SSRF protection. Use safeHttp.axios / safeHttp.createAxios({ ... }) from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
          },
          {
            "selector": "CallExpression[callee.object.name='globalThis'][callee.property.name='fetch']",
            "message": "Raw globalThis.fetch(...) bypasses request-filtering-agent's SSRF protection. Use safeHttp.axios / safeHttp.createAxios({ ... }) from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
          },
          {
            "selector": "CallExpression[callee.object.name='axios'][callee.property.name='create']",
            "message": "axios.create(...) bypasses request-filtering-agent's SSRF protection. Use safeHttp.createAxios({ ... }) from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
          },
          {
            "selector": "CallExpression[callee.name='axios']",
            "message": "Calling axios(...) directly bypasses request-filtering-agent's SSRF protection. Use safeHttp.axios from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
          }
        ]
      }
    },
    {
      "files": ["utils/src/safe-http.ts"],
      "rules": {
        "no-restricted-imports": "off",
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.name='fetch']",
            "message": "Raw fetch(...) bypasses request-filtering-agent's SSRF protection. Use safeHttp.axios / safeHttp.createAxios({ ... }) from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
          },
          {
            "selector": "CallExpression[callee.object.name='globalThis'][callee.property.name='fetch']",
            "message": "Raw globalThis.fetch(...) bypasses request-filtering-agent's SSRF protection. Use safeHttp.axios / safeHttp.createAxios({ ... }) from @aiqadam/server-utils instead. See .claude/rules/safe-http.md."
          }
        ]
      }
    },
    {
      "files": ["**/test/**/*.ts", "**/test/**/*.js"],
      "rules": {
        "no-restricted-imports": "off",
        "no-restricted-syntax": "off"
      }
    }
  ]
}
